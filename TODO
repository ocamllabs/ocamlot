* service
  * github scanning
  * daemonize + control channel
* cmd
  * work directory management
  * switch flag
  * output replay shell script
  * isolate environment (chroot?)
  * scan system compilers
  * persist worker cookie
* opam task
  * metadata check
  * mk-repo check
  * pin version (solved with v specific install?)
  * error classification
    * transient
    * missing dep
    * environmental
* ocaml task
  * monitor ocaml repo
  * install compiler variants and generate tests
* fix opam2web
* policy
  * test downstream
  * bisect constraint search
